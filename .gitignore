# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environments
.venv/
venv/
env/

# Experiment outputs (all under artifacts/ now; artifacts/ is also
# ignored wholesale below). Kept as belt-and-suspenders for stragglers
# if a script ever writes to the repo root instead of artifacts/.
experiments/
outputs/
multirun/
wandb/
.hydra/
*.ckpt
*.pt

# Reference-only third-party code
third_party/

# Pre-computed tokens / embeddings (served from disk, not git)
artifacts/

# Editors
.idea/
.vscode/
