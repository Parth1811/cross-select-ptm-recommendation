epochs: 100
batch_size: 16
lr: 1.0e-4
weight_decay: 0.0
optimizer: adamw
loss:
  ranking_weight: 1.0
  mse_weight: 0.1
log_every: 50
eval_every: 5
checkpoint_dir: experiments/${now:%Y-%m-%d_%H-%M-%S}
